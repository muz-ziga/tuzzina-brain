# Copy values from the server environment. Never commit real secrets.
# Tuzzina org API key (Settings > API in Tuzzina, or server-side lookup).
TUZZINA_API_URL=http://127.0.0.1:4107/api
TUZZINA_API_KEY=
# Optional: pick the Facebook channel when several exist (substring match).
TUZZINA_CHANNEL_MATCH=
# Optional: real AI generation. Empty = mock generators (tests/dry-runs).
OPENAI_API_KEY=
