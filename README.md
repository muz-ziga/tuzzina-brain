# tuzzina-brain — Intelligence/Generation Layer for Tuzzina
# Tuzzina (Postiz) is the Execution Engine. This repo only produces
# inputs (text + public media URLs + dates) and pushes them through
# Tuzzina's Public API. It owns no publish state, no scheduler, no DB.
